# dbx-tools-u2m-postgres

Optional Postgres credential storage adapter for `dbx-tools-u2m`. It persists one token document per profile and serializes refresh with a profile-keyed PostgreSQL advisory lock.

See the [workspace README](../../README.md) for setup and examples.
