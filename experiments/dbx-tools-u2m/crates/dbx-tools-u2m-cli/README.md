# dbx-tools-u2m-cli

Standalone `dbx-tools-u2m` command. Keyring support is enabled by default. Compile with the `postgres` feature to add the separate `dbx-tools-u2m-postgres` adapter and the `--storage postgres` option.

See the [workspace README](../../README.md) for commands and environment variables.
