# dbx-tools-auth-u2m-cli

Standalone `dbx-tools-auth-u2m` command. Keyring support is enabled by default.
Compile with the `postgres` feature to add the CLI's internal Postgres storage
adapter and the `--storage postgres` option.

See the [workspace README](../../../README.md) for commands and environment variables.
