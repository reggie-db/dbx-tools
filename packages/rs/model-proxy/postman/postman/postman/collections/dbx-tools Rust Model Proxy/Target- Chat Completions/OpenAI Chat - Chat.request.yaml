$kind: http-request
name: OpenAI Chat -> Chat
url: "{{baseUrl}}/v1/chat/completions"
method: POST
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "model": "{{chatModel}}",
      "messages": [{"role": "user", "content": "Return the word chat."}],
      "stream": false
    }
order: 1000
