from __future__ import annotations

"""Environment names shared by the launcher and Graphiti server process."""

UPSTREAM_MCP_PATH_ENV = "UPSTREAM_MCP_PATH"
PROCESS_STATE_PATH_ENV = "PROCESS_STATE_PATH"
