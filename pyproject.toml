# ~~ Generated by projen. To modify, edit .projenrc.ts and run "bunx projen".
[project]
name = "dbx-tools-python-workspace"
version = "0.0.0"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = ["pytest>=8.4,<9", "pytest-asyncio>=1.1,<2", "ruff>=0.12,<1"]

[tool.uv]
package = false

[tool.uv.workspace]
members = ["py-packages/*"]

[tool.uv.sources]
dbx-tools-postgres = { workspace = true }
dbx-tools-bus = { workspace = true }

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["py-packages"]

[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint.per-file-ignores]
"py-packages/bus/src/dbx_tools/bus/topic_bus.py" = ["BLE001"]
